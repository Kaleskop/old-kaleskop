<div class="px-2 md:border-l md:border-black">
 <p class="mb-2 font-medium text-2xl">{{ __( 'Business registration' ) }}</p>

 <p class="mb-4">{{ __( 'By registering your business you will have access to your 1st GB of free storage space.' ) }}</p>

 <p class="mb-2">{{ __( 'We are able to manage all companies belonging to the European Economic Area.' ) }}</p>
 <p class="mb-4">{{ __( 'Please, make sure you enter all the data correctly.' ) }}</p>

 <dl class="py-2 md:pl-4">
  <dt class="mb-2 font-medium">{{ __( 'Legal Name' ) }}</dt>
  <dd class="mb-4 text-sm">{{ __( 'Your business legal name must exactly match the one associated with the tax ID. Make sure it is correct, including upper and lower case.' ) }}</dd>

  <dt class="mb-2 font-medium">{{ __( 'Email' ) }}</dt>
  <dd class="mb-4 text-sm">{{ __( 'Enter a valid email address that can receive all company and system communications.' ) }}</dd>

  <dt class="mb-2 font-medium">{{ __( 'VAT Number' ) }}</dt>
  <dd class="text-sm">{{ __( 'Make sure the first two digits of your VAT number match the country of origin you select.' ) }}</dd>
 </dl>
</div>
